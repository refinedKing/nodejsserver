#coding=utf-8
from collections import deque

class Q(object):
	"""docstring for Q
	Python queue 
	"""

	queue = []
	def __init__(self):
	super(Q, self).__init__()


	def q(d):
		if (isinstance(d,int) || isinstance(d,float)) return;
		Q.queue.append(d)
		return Q

	def d():
		s = Q.queue.popleft();
		if (!s) return;
		return s
